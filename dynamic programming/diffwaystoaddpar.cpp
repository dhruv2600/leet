class Solution {
public:
    void helper(string curr,int open,int closed,int ptr,int n)
    {
        if(ptr==n)
        {
            stack<char>stk;
            for(auto ch:curr)
            {

            }
        }
        if(open!=0)
        {

        }

    }
    vector<int> diffWaysToCompute(string exp) {
        int pairs=0;
        for(auto ch:exp)
            if(!isalpha(ch))
                pairs++;

    }
};