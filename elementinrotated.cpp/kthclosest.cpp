class Solution {
public:
    int smallestDistancePair(vector<int>& nums, int k) {
        set<int> s;
        for(auto it=nums.begin();it != nums.end();++it)
        {
          for(int i=0;i<k;i++)
          {
            
          }
        }

    }
};
