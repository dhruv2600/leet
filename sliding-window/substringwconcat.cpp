class Solution {
public:
    vector<int> findSubstring(string s, vector<string>& words) {
        unordered_map<string,int>ht;  //2 if visited,1 if present & not visited, 0 if not present

        for(string word:words)
            ht[word]+=1;
        string temp="";
        for(char c:s)
        {

        }


        
    }
};